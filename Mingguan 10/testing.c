#include <stdio.h>
#include <string.h>
#define ll long long int
#define gtc() getchar()

int main()
{
    char temp[105][25];
    for (int i = 0; i < 101; i++)
    {
        strcpy(temp[i], "free");
    }

    for (int i = 0; i < 10; i++)
    {
        printf("");
    }
    return 0;
}